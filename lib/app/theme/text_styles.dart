import 'package:flutter/material.dart';

class AppTextStyles {
  // Stile per i grandi timer/countdown [cite: 131]
  static const TextStyle bigTimer = TextStyle(
    fontSize: 48,
    fontWeight: FontWeight.bold,
    letterSpacing: -1.0,
  );

  // Stile per header card [cite: 141]
  static const TextStyle cardHeader = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.w600,
  );
}
